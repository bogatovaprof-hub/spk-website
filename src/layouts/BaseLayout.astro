---
import '@fontsource-variable/roboto/wght.css';
import '../styles/tokens.css';
import '../styles/global.css';
import Header from '../components/Header.astro';
import ConsultationDialog from '../components/ConsultationDialog.astro';
import Footer from '../components/Footer.astro';
import { withBase } from '../lib/urls';

interface Props {
  title: string;
  description: string;
  activeSection?: string;
  bodyClass?: string;
}

const {
  title,
  description,
  activeSection = '',
  bodyClass = ''
} = Astro.props;
const resolvedBodyClass = [bodyClass, bodyClass.includes('home-page') ? '' : 'inner-page']
  .filter(Boolean)
  .join(' ');
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="robots" content="noindex, nofollow" />
    <meta name="theme-color" content="#f7f7f4" />
    <link rel="icon" type="image/png" href={withBase('/images/logo-spk-client.png')} />
    <title>{title}</title>
  </head>
  <body class={resolvedBodyClass}>
    <a class="skip-link" href="#main-content">Перейти к содержанию</a>
    <Header activeSection={activeSection} />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
    <ConsultationDialog />
  </body>
</html>
